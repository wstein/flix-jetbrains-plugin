{
  "name": "flix-fallback-grammar",
  "displayName": "Flix (bundled fallback grammar)",
  "description": "Static baseline syntax highlighting for .flix files, derived from wstein/flix-fork's lexer. Used while/if the Flix language server's LSP semantic tokens haven't rendered yet.",
  "contributes": {
    "languages": [
      { "id": "flix", "extensions": [".flix"], "aliases": ["Flix"] }
    ],
    "grammars": [
      { "language": "flix", "scopeName": "source.flix", "path": "syntaxes/flix.tmLanguage.json" }
    ]
  }
}
